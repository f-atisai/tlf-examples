# Population Summary

Simple Python example that creates a population pyramid chart based on the Statology tutorial.

## Run

```bash
cd population-summary
uv run python -m population_summary
```

This will generate a PNG file named `population_pyramid.png` in the project root and show the chart on screen.
